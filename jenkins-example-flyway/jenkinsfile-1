pipeline {
    agent any
    stages {
        stage ('Verify version') {
            steps{
                sh 'pwd'
                sh 'cd /Users/vennikarthik/jenkins/jenkins-data'
                sh 'docker run --rm flyway/flyway'
                sh 'docker run --rm flyway/flyway:8.5.1 version'
            }
        }
    }
}
